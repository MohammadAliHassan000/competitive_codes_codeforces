#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int m;
        vector<int>v1;
        vector<int>v2;
        for(int i=0;i<m;i++)
        {
            int temp;
            cin>>temp;
            v1.push_back(temp);
            for(int j=0;j<v1[i];j++)
            {
                int t2;
                cin>>t2;
                v2.push_back(t2);
            }

        }

        unordered_map<int,int>m;
        vector<int>result;
        for(int i=0;i<v2.size();i++)
        {
            while()
            {
                
            }
        }


    }
    return 0;
}
